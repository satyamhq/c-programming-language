/* Recursion in C
   Recursion is when a function calls itself to solve a smaller
   version of the same problem, until it reaches a base case.

   Basic Structure:

   return_type function(params) {
       if (base_case) return result;      // STOP condition
       return function(smaller_problem);  // Recursive call
   }
*/
